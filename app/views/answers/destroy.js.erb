<% if @error %>
  alert("<%= @error %>");
<% else %>
  $(".answers-panel").html("<%= escape_javascript render(@answers) %>");
  $('.answer-area').val("");
  $('.answer-count').html("<%= escape_javascript pluralize(@question.answers.count, "Answer")%>");
<% end %>